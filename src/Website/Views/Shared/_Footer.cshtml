@model SiteOptions
<hr />
<footer>
    <p class="d-none d-md-block">
        &copy; @Model.Metadata.Author.Name @DateTimeOffset.UtcNow.Year |
        <a id="link-status-d" href="@Model.ExternalLinks.Status.AbsoluteUri" rel="noopener" target="_blank" title="View site uptime information">
            Site Status &amp; Uptime
        </a>
        | Built from
        <a id="link-git-commit" href="@Model.Metadata.Repository/commit/@GitMetadata.Commit" title="View commit @GitMetadata.Commit on GitHub">
            @string.Join(string.Empty, GitMetadata.Commit.Take(7))
        </a>
        on
        <a id="link-git-branch" href="@Model.Metadata.Repository/tree/@GitMetadata.Branch" title="View branch @GitMetadata.Branch on GitHub">
            @GitMetadata.Branch
        </a>
        <span id="build-date" data-format="YYYY-MM-DD HH:mm Z" data-timestamp="@GitMetadata.Timestamp.ToString("u", CultureInfo.InvariantCulture)"></span>
    </p>
    <div class="d-md-none">
        <p>
            &copy; @Model.Metadata.Author.Name @DateTimeOffset.UtcNow.Year |
            <a id="link-status-m" href="@Model.ExternalLinks.Status.AbsoluteUri" rel="noopener" target="_blank" title="View site uptime information">
                Site Status &amp; Uptime
            </a>
        </p>
    </div>
</footer>
